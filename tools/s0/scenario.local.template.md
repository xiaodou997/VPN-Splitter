# S0 本机场景清单（填写后保留在 .local/，不要提交）

状态：NOT VERIFIED。真实地址、应用、域名、客户名称和原始证据只保存在本机；不要填入密码、私钥或令牌。

## 已接受的选择

主场景 Managed Include；同时验收 Managed Bypass；External 首发仅 Bypass。允许受限 DNS-derived；不要求系统级 Kill Switch、完整 IPv6、按 App 或严格域名隔离。无需重新选择这些产品边界。

## 环境（填写事实，不猜测）

- 测试日期/时区：
- 采集位置：宿主 Mac / VM 内 / UNKNOWN：
- macOS 版本及 build、CPU：
- 被测客户端版本、连接方式：
- Wi-Fi/有线、是否有其他隧道/代理/网络扩展：
- before、after、reverted 快照目录（本地）：
- 本轮允许的测试操作、恢复入口和授权范围：

## 原工作流的分工

- VPN：认证/设备检查/其他约束是否必须依赖官方客户端？UNKNOWN 也可。
- VM：只用于隔离全局 VPN，还是还承担业务程序/额外服务？
- gost：提供哪种本地代理及 TCP/UDP 能力？只描述结构，不复制含凭据 URL。
- 本机规则层：是否仍使用额外 TUN、Fake-IP、按 App、规则订阅、自动回退？
- .conf/.ovpn：本机是否存在；是否实际导入/认证成功；不上传文件。

## 实际验收目标

| 代号 | 本机填写的真实应用/地址与协议 | 期望路径 | 选择 Include/Bypass 的场景 | 可验证出口的方式 | 当前结果 |
| --- | --- | --- | --- | --- | --- |
| VPN-01 | | VPN | | 受控响应/服务端记录/授权观察 | NOT RUN |
| DIRECT-01 | | DIRECT | | 受控响应/服务端记录/授权观察 | NOT RUN |
| DEFAULT-01 | | 按当前模式默认 | | 不与例外复用同一目标 | NOT RUN |
| DNS-01 | | 指定 resolver | | 授权 DNS 记录；与数据路径分开 | NOT RUN |

既有 TCP/QUIC 连接与新连接分开记录。目标可访问不等于出口正确；不默认查询第三方公网 IP 服务。

## 范围差异与替代判定

按 App、任意后缀、Fake-IP、地区规则集、代理自动回退等不能假设被 v0.1 等价覆盖。逐项写“无需保留 / 改为明确目标规则 / 阻断替代 / UNKNOWN”，不悄悄删除需求。

| 原能力 | v0.1 能否覆盖及限制 | 用户实际仍需要吗 | 证据/阻断原因 |
| --- | --- | --- | --- |
| | UNKNOWN | UNKNOWN | |

## S0-03 前置检查（全部满足前不写路由）

- [ ] 当前物理路径和被测隧道身份清楚，没有未解释歧义。
- [ ] 有授权的 VPN/DIRECT 目标和实际出口证明办法。
- [ ] 明确单目标修改、所有权检查、撤销与异常恢复方案。
- [ ] 未发现需要绕过的强制安全策略；不关闭企业安全组件。

结论：BLOCKED / READY FOR AUTHORIZED TEST。不是 COMPATIBLE 或 VM 可移除证明。
